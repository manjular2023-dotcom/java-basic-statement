import java.util.*;
public class Main {
      public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int a = scanner.nextInt();
        System.out.println("Enter Danny's basic salary:");
        double HRA;
        double DA;

        if (a < 15000) {
            HRA = 0.15 * a;
            DA = 0.90 * a;
        } else {
            HRA = 5000;
            DA = 0.98 * a;
        }

        double b = a + HRA + DA;
      DecimalFormat df = new DecimalFormat("#.00");
        System.out.println("b: " + df.format(b));
    }
}
